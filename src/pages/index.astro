---
const base = import.meta.env.BASE_URL ?? "/";
const year = new Date().getFullYear();
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <title>AmicBridge – Trust Before Money</title>
    <meta
      name="description"
      content="AmicBridge is a simple, structured place to lend, borrow and invest with written agreements and clear profit sharing."
    />
    <meta name="viewport" content="width=device-width, initial-scale=1" />

    <style>
      :root {
        --bg: #f3f4f6;
        --white: #ffffff;
        --primary: #1d4ed8;
        --border: #d1d5db;
        --text-main: #111827;
        --text-muted: #6b7280;
        --accent: #16a34a;
      }

      * { box-sizing: border-box; }

      body {
        margin: 0;
        font-family: system-ui, -apple-system, Segoe UI, Roboto, Arial, sans-serif;
        background: var(--bg);
        color: var(--text-main);
      }

      a { color: var(--primary); text-decoration: none; }
      a:hover { text-decoration: underline; }

      header {
        background: var(--white);
        border-bottom: 1px solid var(--border);
        padding: 16px 32px;
        display: flex;
        align-items: center;
        justify-content: space-between;
      }

      .logo {
        font-weight: 800;
        font-size: 20px;
        letter-spacing: 0.08em;
        text-transform: uppercase;
      }
      .logo span { color: var(--primary); }

      nav {
        display: flex;
        gap: 20px;
        font-size: 14px;
      }

      nav a {
        color: var(--text-main);
        font-weight: 500;
      }

      .container {
        max-width: 960px;
        margin: 0 auto;
        padding: 32px 16px 56px;
      }

      .hero {
        margin-top: 24px;
        display: grid;
        grid-template-columns: minmax(0, 3fr) minmax(0, 2fr);
        gap: 32px;
        align-items: center;
      }

      .trust-tag {
        display: inline-flex;
        align-items: center;
        gap: 6px;
        padding: 4px 10px;
        border-radius: 999px;
        background: #e0f2fe;
        color: #0369a1;
        font-size: 12px;
        font-weight: 600;
        text-transform: uppercase;
        letter-spacing: 0.08em;
        margin-bottom: 10px;
      }

      .hero h1 {
        margin: 0 0 8px;
        font-size: 34px;
      }

      .hero p {
        margin: 0 0 20px;
        font-size: 15px;
        color: var(--text-muted);
        max-width: 480px;
      }

      .hero-cta {
        display: flex;
        gap: 12px;
        flex-wrap: wrap;
      }

      .btn-primary {
        border: 1px solid var(--primary);
        background: var(--primary);
        color: #fff;
        padding: 10px 18px;
        border-radius: 999px;
        font-size: 14px;
        font-weight: 600;
        cursor: pointer;
      }

      .btn-secondary {
        border: 1px solid var(--border);
        background: var(--white);
        color: var(--text-main);
        padding: 10px 18px;
        border-radius: 999px;
        font-size: 14px;
        cursor: pointer;
        font-weight: 500;
      }

      .hero-card {
        background: var(--white);
        border-radius: 8px;
        border: 1px solid var(--border);
        padding: 16px 18px;
        font-size: 13px;
      }

      .hero-card h3 {
        margin: 0 0 10px;
        font-size: 15px;
      }

      .pill-row {
        display: flex;
        flex-wrap: wrap;
        gap: 8px;
      }

      .pill {
        padding: 4px 10px;
        border-radius: 999px;
        border: 1px solid var(--border);
        background: #f9fafb;
        font-size: 12px;
        color: var(--text-muted);
      }

      h2.section-title {
        font-size: 20px;
        margin-top: 40px;
        margin-bottom: 12px;
      }

      .grid-3 {
        display: grid;
        grid-template-columns: repeat(3, minmax(0, 1fr));
        gap: 16px;
      }

      .card {
        background: var(--white);
        border-radius: 8px;
        border: 1px solid var(--border);
        padding: 14px 16px;
        font-size: 14px;
      }

      .card h3 {
        margin: 0 0 6px;
        font-size: 15px;
      }

      .card p {
        margin: 0;
        color: var(--text-muted);
      }

      .list {
        margin: 6px 0 0;
        padding-left: 18px;
        color: var(--text-muted);
        font-size: 13px;
      }

      .list li {
        margin-bottom: 3px;
      }

      .two-col {
        display: grid;
        grid-template-columns: minmax(0, 3fr) minmax(0, 2fr);
        gap: 18px;
      }

      .badge-row {
        display: flex;
        flex-wrap: wrap;
        gap: 8px;
        margin-top: 6px;
      }

      .badge {
        font-size: 11px;
        padding: 4px 10px;
        border-radius: 999px;
        background: #ecfdf3;
        border: 1px solid #bbf7d0;
        color: #166534;
      }

      .notice {
        background: #fef2f2;
        border-radius: 8px;
        border: 1px solid #fee2e2;
        padding: 12px 14px;
        font-size: 12px;
        color: #991b1b;
      }

      .contact-card {
        background: var(--white);
        border-radius: 8px;
        border: 1px solid var(--border);
        padding: 16px 18px;
        font-size: 14px;
      }

      footer {
        border-top: 1px solid var(--border);
        padding: 14px 24px;
        font-size: 12px;
        background: var(--white);
        color: var(--text-muted);
      }

      @media (max-width: 900px) {
        header {
          padding: 12px 16px;
        }
        .hero {
          grid-template-columns: 1fr;
        }
        .grid-3 {
          grid-template-columns: 1fr;
        }
        .two-col {
          grid-template-columns: 1fr;
        }
        .hero h1 {
          font-size: 28px;
        }
      }
    </style>
  </head>

  <body>
    <header>
      <div class="logo">AMIC<span>BRIDGE</span></div>

      <nav>
        <a href="#how">How it works</a>
        <a href="#trust">Trust</a>
        <a href="#currency">Currency</a>
        <a href="#contact">Contact</a>
      </nav>
    </header>

    <div class="container">
      <!-- HERO -->
      <section class="hero">
        <div>
          <div class="trust-tag">✔ TRUST BEFORE MONEY</div>

          <h1>Lend. Borrow. Invest. Share profit.</h1>

          <p>
            AmicBridge is a simple network where people lend, borrow and invest
            with written agreements and shared profit. No hype. No noise. Just structure.
          </p>

          <div class="hero-cta">
            <a href="#contact">
              <button type="button" class="btn-primary">Contact CEO</button>
            </a>
            <a href="#how">
              <button type="button" class="btn-secondary">How it works</button>
            </a>
          </div>
        </div>

        <aside class="hero-card" aria-label="Key facts">
          <h3>What AmicBridge stands for</h3>
          <div class="pill-row">
            <span class="pill">Written loan agreements</span>
            <span class="pill">Clear profit sharing</span>
            <span class="pill">No hidden fees</span>
            <span class="pill">Human-to-human network</span>
          </div>
        </aside>
      </section>

      <!-- HOW IT WORKS -->
      <section id="how">
        <h2 class="section-title">How it works</h2>

        <div class="grid-3">
          <div class="card">
            <h3>1. Lend</h3>
            <p>Provide capital to someone in the AmicBridge network on agreed terms.</p>
          </div>

          <div class="card">
            <h3>2. Borrow</h3>
            <p>Get funding for projects or needs with a clear repayment plan.</p>
          </div>

          <div class="card">
            <h3>3. Share profit</h3>
            <p>When things go well, profit is shared exactly as written in the agreement.</p>
          </div>
        </div>
      </section>

      <!-- TRUST & AGREEMENTS -->
      <section id="trust">
        <h2 class="section-title">Trust & agreements</h2>

        <div class="two-col">
          <div>
            <p style="font-size:14px; color:var(--text-muted);">
              Every deal made through the AmicBridge network is backed by a simple,
              written agreement. First you agree on the terms. Then money moves.
            </p>

            <ul class="list">
              <li>Loan agreements for lending deals</li>
              <li>Investment / profit-sharing agreements</li>
              <li>Clear dates, amounts and responsibilities</li>
            </ul>

            <div class="badge-row">
              <span class="badge">Trust before money</span>
              <span class="badge">Agreement before action</span>
            </div>
          </div>

          <div class="notice">
            <strong>Risk reminder</strong><br />
            This is not a traditional bank. Any lending or investing can lead to
            loss of money. Only use money you can afford to risk.
          </div>
        </div>
      </section>

      <!-- CURRENCY -->
      <section id="currency">
        <h2 class="section-title">Currency choice</h2>

        <div class="card">
          <p style="margin:0 0 6px; font-size:14px; color:var(--text-muted);">
            Deals inside the AmicBridge network can be made in different currencies,
            depending on what both sides agree on.
          </p>

          <ul class="list">
            <li>Crypto</li>
            <li>NOK – Norwegian Krone</li>
            <li>SEK – Swedish Krona</li>
            <li>EUR – Euro</li>
            <li>USD – United States Dollar</li>
          </ul>
        </div>
      </section>

      <!-- CONTACT -->
      <section id="contact">
        <h2 class="section-title">Contact</h2>

        <div class="two-col">
          <div class="contact-card">
            <h3>Talk directly with the CEO</h3>
            <p style="margin:0 0 10px;">
              AmicBridge is led by <strong>robert & Habbitx</strong>.
            </p>

            <p style="margin:0 0 6px; font-size:14px;">
              Email:
              <a href="mailto:robertamicolsen@gmail.com">
                robertamicolsen@gmail.com
              </a>
            </p>

            <p style="margin:4px 0 0; font-size:13px; color:var(--text-muted);">
              Write if you want to lend, borrow or invest. Keep it short and clear.
            </p>
          </div>

          <div class="card">
            <h3>What to include</h3>
            <ul class="list">
              <li>Are you lending, borrowing or investing?</li>
              <li>Approximate amount</li>
              <li>Preferred currency</li>
              <li>Time frame (months / years)</li>
            </ul>
          </div>
        </div>
      </section>
    </div>

    <footer>
      © {year} AmicBridge · Trust Before Money · This page is informational only and not financial advice.
    </footer>
  </body>
</html>
